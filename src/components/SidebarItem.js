import React, { Component } from 'react';
import { connect } from 'react-redux';

export class SidebarItem extends Component {
    render() {
        return (
            <div className="sidebar-div">Sidebar item div</div>
        );
    }
}